import omnomnom from '../../../utils/omnomnom';
import { module, test } from 'qunit';

module('Unit | Utility | omnomnom');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = omnomnom();
  assert.ok(result);
});
